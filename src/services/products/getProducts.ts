// Dependencias.

// Handler.
const handler = async () => {
  return new Promise((resolve, reject) => {
    try {
    } catch (error) {
      console.error(error);
    }
  });
};

// Exportación.
export default handler;
