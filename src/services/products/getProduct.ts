// Dependencias.

// Handler.
const handler = async (product_id: string) => {
  return new Promise((resolve, reject) => {
    try {
    } catch (error) {
      console.error(error);
    }
  });
};

// Exportación.
export default handler;
