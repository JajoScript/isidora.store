// Dependencias.
import React from "react";

// Estilos.
import "./Template.styles.css";

// Definición del componente: <Template />.
const Template = (props: any) => {
  // 1. Manejo del estado.
  const {} = props;

  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render.
  return (
    <React.Fragment>
      <h1>Template</h1>
    </React.Fragment>
  );
};

// Proptypes.
Template.propTypes = {};

// Exportación.
export default Template;
