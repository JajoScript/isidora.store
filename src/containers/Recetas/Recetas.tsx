// Dependencias.
import React from "react";
import RecetasPage from "../../pages/Recetas/Recetas";

// Props.
interface Props {}

// Definición del contenedor: <RecetasContainer />
const RecetasContainer = (props: Props) => {
  // 1. Manejo del estado.
  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render
  return <RecetasPage />;
};

// Proptypes.
RecetasContainer.propTypes = {};

// Exportación.
export default RecetasContainer;
