// Dependencias.
import React from "react";

const RecetasContainer = (props: any) => {
  // 1. Manejo del estado.
  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render
  return (
    <React.Fragment>
      <h1>RECETAS</h1>
    </React.Fragment>
  );
};

// Proptypes.
RecetasContainer.propTypes = {};

// Exportación.
export default RecetasContainer;
