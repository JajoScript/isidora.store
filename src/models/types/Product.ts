// Dependencias.
// ...

// Definición.
interface Product {
  id: string;
  name: string;
  price: number;
  brand: string;
  description: string;
  ingredients: string;
  image: string;
}

// Exportación.
export type { Product };
